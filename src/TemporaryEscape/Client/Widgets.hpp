#include "../Gui/GuiContext.hpp"
#include "../Server/Schemas.hpp"

namespace Engine::Widgets {
extern void ENGINE_API loading(GuiContext& gui, const Vector2i& viewport, const std::string& message, float progress);
}
