#include "../Gui/GuiContext.hpp"
#include "../Server/Schemas.hpp"

namespace Scissio::Widgets {
extern void SCISSIO_API loading(GuiContext& gui, const Vector2i& viewport, const std::string& message, float progress);
}
