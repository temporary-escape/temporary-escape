category: Blocks
label: Engine
particles:
  type: Thruster
  startColor: [ 1.0, 0.0, 0.0, 1.0 ]
  endColor: [ 0.0, 0.0, 1.0, 1.0 ]
shapes:
  - Cube
materials:
  - faces:
      - PositiveX
      - NegativeX
      - PositiveY
      - NegativeY
      - PositiveZ
    baseColor:
      texture: texture_hull_diff
    normal:
      texture: texture_hull_norm
    metallicRoughness:
      texture: texture_hull_meta
    mask:
      texture: texture_hull_mask
  - faces:
      - NegativeZ
    baseColor:
      texture: texture_engine_diff
    normal:
      texture: texture_engine_norm
    metallicRoughness:
      texture: texture_engine_meta
    emissive:
      texture: texture_engine_emis
    mask:
      texture: texture_engine_mask
