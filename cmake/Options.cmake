set(TEMPORARY_ESCAPE_BUILD_TESTS
        ON
        CACHE BOOL
        "Build with tests")

set(TEMPORARY_ESCAPE_LLVM_SYMBOLIZER_PATH
        ""
        CACHE STRING
        "Path to the llvm-symbolizer to enable address sanitizer")
